apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  containers:
    - name: hello-world
      image: irakevich/hello-world:tagVersion
      ports:
        - containerPort: 8080